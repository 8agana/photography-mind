# Roadmap (short)

- Harden HTTP MCP surface (authn/bearer) if exposing beyond localhost/tunnel.
- Expand automated validation/health checks for gallery status workflows and ShootProof sync.
- Add regression tests around family/sibling parsing to prevent split families on import.
